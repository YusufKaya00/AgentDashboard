{
  "name": "tnega-agent-dashboard",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "postinstall": "npm install --prefix backend-node --ignore-scripts",
    "dev": "concurrently --kill-others-on-fail --names web,api \"next dev -H 127.0.0.1 -p 3000\" \"npm run dev --prefix backend-node\"",
    "build": "next build",
    "start:web": "next start -H 127.0.0.1 -p 3000",
    "start": "concurrently --kill-others-on-fail --names web,api \"npm run start:web\" \"npm run start --prefix backend-node\"",
    "local": "npm run build && npm run start",
    "lint": "eslint",
    "verify": "npm run lint && npm test --prefix backend-node && npm run build"
  },
  "dependencies": {
    "lucide-react": "^1.27.0",
    "next": "16.2.12",
    "react": "19.2.4",
    "react-dom": "19.2.4"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "concurrently": "^9.2.1",
    "eslint": "^9",
    "eslint-config-next": "16.2.12",
    "tailwindcss": "^4",
    "typescript": "^5"
  }
}
